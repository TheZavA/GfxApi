#version 400

in vec3 normal;

uniform sampler2D tex;

out vec4 frag_colour;

void main () 
{
	float intensity = dot(vec3(0.3,1.0,0.3), normal);
	
	frag_colour = texture(tex, vec2(normal.x*100,normal.z*100)) * intensity ;
}